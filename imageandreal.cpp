#include <stdio.h>
#include <conio.h>
struct karmasik {
	int real;
	int img;
} typedef tanim;
int main(){
tanim a,b,c;
printf("a icin real ve img girin");
scanf("%d%d",&a.real,&a.img);
printf("b icin real ve img girin");
scanf("%d%d",&b.real,&b.img);	
c.real = a.real+b.real;
c.img = a.img + b.img;
printf("c karmasik sayisi = %d+%di",c.real,c.img);

getche();


}
